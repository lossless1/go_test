{
  "name": "go_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "WATCHFILE=1 nodemon --exec \"npm run build && npm run start\" --watch ./src/$WATCHFILE/$WATCHFILE.go",
    "build": "cd src/$WATCHFILE && go build",
    "start": "./src/$WATCHFILE/$WATCHFILE",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.19.3"
  }
}
